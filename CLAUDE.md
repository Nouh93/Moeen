# CLAUDE.md — إرشادات العمل في مستودع مُعين

## ما هو المشروع
مُعين: منصة تجارة وتوصيل لليمن. الأولوية القصوى لصاحب المنتج:
**الثبات عند التوسّع + منطق مالي سليم 100%**. التزم بهذا في كل قرار.

## القواعد المالية غير القابلة للتفاوض
- **لا أعداد عائمة (float) للمال.** استخدم `Money` من `@moeen/ledger` (bigint + scale).
- **قيد مزدوج فقط.** كل حركة مالية = قيد متوازن عبر `Ledger.post(...)`؛ لا تعدّل
  الأرصدة مباشرة.
- **لا رصيد سالب** على حسابات التاجر (الحد = 0). العملية تُرفض ذرّياً عند عدم الكفاية.
- **idempotency إلزامي** لأي حدث مالي وارد (مفتاح فريد).
- أي منطق مالي جديد يجب أن يأتي معه اختبارات في `packages/ledger`.

## المكدّس
- Monorepo (pnpm). TypeScript صارم. NestJS (خلفية). PostgreSQL + Prisma. Redis + BullMQ.
  Next.js PWA (واجهة). راجع `docs/adr/`.

## الأوامر
- `pnpm install` — التثبيت.
- `pnpm ledger:test` — اختبارات القلب المالي.
- `pnpm -r test` / `pnpm -r typecheck` — على كل الحزم.
- `docker compose up -d` — PostgreSQL + Redis.

## أسلوب العمل
- اقرأ الـPRD ذا الصلة (`docs/PRD-Yemen-*.md`) قبل تنفيذ ميزة مالية/تشغيلية.
- النصوص الموجّهة للمستخدم بالعربية ووفق دليل اللغة (القسم 26 من الـPRD).
- وثّق القرارات المعمارية المهمة كـADR جديد في `docs/adr/`.
- لا تضع أسراراً في الكود؛ استخدم `.env` (انظر `.env.example`).
