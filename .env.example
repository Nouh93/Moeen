# انسخ هذا الملف إلى .env واملأ القيم. لا تضع أسراراً حقيقية في .env.example.

# قاعدة البيانات (تطابق docker-compose.yml)
DATABASE_URL="postgresql://moeen:moeen_dev@localhost:5432/moeen?schema=public"

# طوابير المهام
REDIS_URL="redis://localhost:6379"

# الخلفية
NODE_ENV="development"
PORT="3000"

# بوابات الدفع والمحافظ (تُملأ عند توفّر اتفاقات onboarding — راجع PRD 24.7)
MEPS_API_KEY=""
ESADAD_BILLER_ID=""
WHATSAPP_API_TOKEN=""
